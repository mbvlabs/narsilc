version: "2"
sql:
  - schema: "schema.sql"
    queries: "query.sql"
    engine: "postgresql"
    gen:
      go:
        package: "queries"
        out: "db"
        sql_package: "pgx/v5"
        omit_sqlc_version: true
        row_mapping: andurel
