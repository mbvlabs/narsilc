version: "2"
sql:
  - engine: "postgresql"
    schema: "schema.sql"
    queries: "query.sql"
    gen:
      go:
        package: "queries"
        out: "db"
        sql_package: "database/sql"
        omit_sqlc_version: true
        row_mapping: andurel
